# Keep every kebin.dev host out of search engines: header for crawlers that honor it, robots.txt for the rest.
add_header X-Robots-Tag "noindex, nofollow, noarchive, nosnippet" always;
location = /robots.txt {
    default_type text/plain;
    return 200 "User-agent: *\nDisallow: /\n";
}
